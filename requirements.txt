# Core Dependencies (ARM64-optimized)
pygame==2.5.0        # Prebuilt with SDL2 support for M1/M2
numpy==1.26.4        # ARM64-accelerated via OpenBLAS
matplotlib==3.8.0    # Python 3.11 compatibility

# Machine Learning Stack
pandas==2.0.3        # Arrow-backed DataFrame support
torch==2.0.0         # MPS backend for Metal GPU acceleration
    # Install command: pip install torch --extra-index-url https://download.pytorch.org/whl/cpu

# Development Tools
pytest==7.4.3        # Async test support
jupyter==1.0.0       # Core notebook package
black==23.9.1        # Modern code formatting
flake8==6.0.0        # Enhanced linting

# System Constraints
--only-binary :all:  # Force precompiled binaries
--use-pep517         # Modern build system

# Installation Procedure
# 1. Create fresh environment
# python -m venv .venv && source .venv/bin/activate

# 2. Upgrade build tools
# pip install -U pip==23.3.1 setuptools==68.2.2 wheel==0.41.2

# 3. Install with dependency resolution
# pip install -r requirements.txt
